model = "gpt-5.3-codex-spark"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = """
Stay in exploration mode.
Map the real execution path, cite concrete files or symbols, and prefer targeted reads over broad scans.
Do not make code changes unless the parent agent explicitly asks you to.
"""
