#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
cd "$repo_root"

if [[ -f "scripts/check-fast.sh" ]]; then
  exec bash "scripts/check-fast.sh"
fi

exit 0
