{
  "version": 1,
  "description": "Managed overlay of global Claude Code settings keys. sync-claude.py merges these blocks into ~/.claude/settings.json so Claude-bundled plugin enablement and bundled-skill visibility stay reproducible across machines and tracked in git. This is NOT the Codex plugin registry (plugins/registry.json, Codex-native) nor the managed standalone skill registry (skills/registry.json); it governs Anthropic-bundled Claude runtime surfaces those registries do not own. Only the keys declared here are managed; any other key already in settings.json is preserved untouched. repoSettings declares managed per-repo keys (repo name -> settings) written into <repo>/.claude/settings.json next to the managed hooks block; keys a repo does not declare are preserved untouched there too.",
  "enabledPlugins": {
    "anthropic-skills@inline": false
  },
  "skillOverrides": {
    "fewer-permission-prompts": "off",
    "keybindings-help": "off",
    "loop": "off",
    "review": "off",
    "security-review": "off",
    "init": "off",
    "code-review": "off",
    "simplify": "off",
    "verify": "off",
    "run": "off",
    "schedule": "off",
    "update-config": "off"
  },
  "repoSettings": {
    "adi": {
      "autoMemoryEnabled": false
    },
    "angie": {
      "autoMemoryEnabled": false
    }
  }
}
