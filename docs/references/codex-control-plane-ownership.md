# Codex Control Plane Ownership

This document is the exact ownership reference for the Codex side of the shared agent control plane.

Use [Codex Control Plane](/Users/dobby/GitHub/agents/docs/architecture/codex-control-plane.md) for the high-level shape and this file for concrete keep / move / generate decisions.

## Ownership Rules

- `~/GitHub/agents` owns canonical, synced, durable control-plane inputs.
- `~/.agents/skills` owns generated Codex user-scope skill links only.
- `~/GitHub/scripts` owns generic machine bootstrap entrypoints and shared shell glue that are not agent-client-owned.
- `~/.codex` owns live Codex runtime state and generated applied outputs.
- `~/.claude` owns live Claude Code runtime state and generated applied outputs.
- Repo-local `.codex/`, `.claude/`, and `.agents/` own project-specific agent surfaces.

## Keep / Move / Generate

### Keep in `~/GitHub/agents`

- architecture, ownership, and operations docs
- managed scripts for Codex, Claude Code, shared hooks, skills, plugins, MCPs, dashboard, and client sync
- `config/global.agents.md`
- `skills/registry.json` and `skills-source/...`
- `plugins/registry.json`
- `mcp/config/presets.json`
- `hooks/registry.json`, `hooks/scripts/`, and `hooks/git/`
- `codex/config/*`
- `dev-servers/registry.json`
- dashboard source and served dashboard assets
- post-sync reconcile logic that decides whether a new `~/GitHub/agents` revision must be applied on a machine

### Keep in `~/.agents`

- `skills/` generated by `scripts/sync-skills-registry.sh`
- no full repo checkout symlink
- no dashboard, canonical registries, hooks, scripts, docs, or client-specific config

### Keep in `~/GitHub/scripts`

- fresh-machine bootstrap entrypoints
- generic setup flows that may call the agent control plane
- generic launchd/scheduler entrypoints such as git auto-sync that may invoke control-plane reconcile scripts

### Keep in `~/.codex`

- `auth.json`
- `history.jsonl`
- `sessions/`
- `log/`
- `sqlite/`
- `state_*.sqlite`
- `shell_snapshots/`
- caches and temp files
- live `config.toml`
- live `hooks.json`
- runtime-installed skills and generated runtime artifacts
- runtime migration markers and indexes such as `.personality_migration`, `session_index.jsonl`, and runtime SQLite files
- Codex-managed vendor imports such as `vendor_imports/skills`, which is a nested Git checkout used by Codex App for recommended skills

### Generate or Sync Into `~/.codex`

- `~/.codex/AGENTS.md` from `config/global.agents.md`
- managed `config.toml` sections sourced from canonical files in `~/GitHub/agents`
- global `hooks.json` from `hooks/registry.json`
- any generated wrappers needed for hook or apply flows

### Generate or Sync Into Repos

- repo `.codex/config.toml`
- repo `.codex/hooks.json`
- repo `.agents/skills/<skill>` links
- repo `.claude/skills/<skill>` links
- repo `.claude/CLAUDE.md` import bridge with `@../AGENTS.md`
- repo `.claude/launch.json` for repos listed in `dev-servers/registry.json`
- repo `.codex/environments/environment.toml` for repos listed in `dev-servers/registry.json`

Agent preview ports are canonical in `dev-servers/registry.json` and generated
for both Claude Code and Codex. Public Cloudflare/LaunchAgent service ports stay
owned by `~/GitHub/scripts` and the owning app repos.

## Current Notable Files

### `~/.codex`

- [config.toml](/Users/dobby/.codex/config.toml): live machine config; target is generated/applied, not hand-owned as the canonical source.
- live `config.toml` no longer owns hook automation; global `~/.codex/hooks.json` and repo-local `.codex/hooks.json` files are rendered from [hooks/registry.json](/Users/dobby/GitHub/agents/hooks/registry.json).
- [vendor_imports/skills](/Users/dobby/.codex/vendor_imports/skills): runtime-managed nested Git checkout from `openai/skills`; do not delete or flatten it during runtime cleanup.

### `~/GitHub/scripts`

- [setup/bootstrap-machine.sh](/Users/dobby/GitHub/scripts/setup/bootstrap-machine.sh): generic machine bootstrap entrypoint that may invoke the agent control plane.
- [setup/codex/AGENTS.md](/Users/dobby/GitHub/scripts/setup/codex/AGENTS.md): describes only the generic shared zshrc layer that remains there.
- [setup/codex/zshrc.shared](/Users/dobby/GitHub/scripts/setup/codex/zshrc.shared): generic shared shell bootstrap that can source the Codex shell fragment from `~/GitHub/agents`.

### `~/GitHub/agents`

- [AGENTS.md](/Users/dobby/GitHub/agents/AGENTS.md): local guidance for this repo.
- [docs/architecture/codex-control-plane.md](/Users/dobby/GitHub/agents/docs/architecture/codex-control-plane.md): canonical high-level design.
- [scripts/bootstrap-machine-agent-control-planes.sh](/Users/dobby/GitHub/agents/scripts/bootstrap-machine-agent-control-planes.sh): canonical machine-facing bootstrap entrypoint.
- [scripts/auto-apply-agent-control-planes.sh](/Users/dobby/GitHub/agents/scripts/auto-apply-agent-control-planes.sh): canonical machine-facing post-sync reconcile entrypoint.
- [codex/scripts/bootstrap-machine-codex.sh](/Users/dobby/GitHub/agents/codex/scripts/bootstrap-machine-codex.sh): canonical Codex-specific machine bootstrap entrypoint.
- [codex/scripts/auto-apply-codex-control-plane.sh](/Users/dobby/GitHub/agents/codex/scripts/auto-apply-codex-control-plane.sh): low-level Codex-specific post-sync reconcile helper.
- [codex/scripts/sync-trusted-projects.sh](/Users/dobby/GitHub/agents/codex/scripts/sync-trusted-projects.sh): trusted-repo sync for global Codex config.
- [codex/scripts/sync-repo-codex-configs.sh](/Users/dobby/GitHub/agents/codex/scripts/sync-repo-codex-configs.sh): repo-local Codex config sync for managed repos.
- [codex/config/repo-bootstrap.json](/Users/dobby/GitHub/agents/codex/config/repo-bootstrap.json): canonical repo bootstrap registry.
- [mcp/config/presets.json](/Users/dobby/GitHub/agents/mcp/config/presets.json): canonical shared MCP definitions.
- [plugins/registry.json](/Users/dobby/GitHub/agents/plugins/registry.json): canonical native Codex plugin registry.

## Ongoing Direction

- Keep `~/GitHub/agents` as the durable source for agent policy, config templates, managed scripts, skills, plugins, MCPs, hooks, dashboard, and repo bootstrap state.
- Keep `~/.agents` thin, with `~/.agents/skills` as the Codex user-scope discovery surface.
- Keep `~/GitHub/scripts` focused on generic machine bootstrap and shared shell glue that can call into this control plane.
- Keep `~/.codex` and `~/.claude` as applied runtime homes for auth, sessions, generated config, logs, caches, and runtime-managed state.
- Move any newly discovered durable policy out of runtime paths and into this repo.
