interface:
  display_name: "Codex Agent Loop"
  short_description: "How Codex prompt layering and loops work"
  default_prompt: "Use $codex-agent-loop to understand how Codex builds prompts, loops over tools, and where model_instructions_file, AGENTS.md, and App Server primitives fit."
