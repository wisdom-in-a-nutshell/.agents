interface:
  display_name: "Codex Agent Loop"
  short_description: "How Codex builds prompts and runs the agent loop"
  default_prompt: "Use $codex-agent-loop to recover how Codex assembles the initial request, iterates through tool calls within a turn, grows conversation state across turns, and where model_instructions_file and AGENTS.md enter."
