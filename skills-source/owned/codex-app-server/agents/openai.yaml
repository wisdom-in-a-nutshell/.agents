interface:
  display_name: "Codex App Server"
  short_description: "Build this repo around Codex App Server primitives."
  default_prompt: "Use $codex-app-server to implement or review this Codex App Server integration, starting with local references and probing installed-build behavior when needed."
