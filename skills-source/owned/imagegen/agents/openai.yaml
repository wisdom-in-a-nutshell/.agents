interface:
  display_name: "Image Gen"
  short_description: "Generate polished images with self-review and reusable styles"
  icon_small: "./assets/imagegen-small.svg"
  icon_large: "./assets/imagegen.png"
  default_prompt: "Generate or edit images for this task using the skill workflow: check `styles/` for a relevant visual family, structure the prompt clearly, create or update a project-local worklog for non-trivial work, inspect outputs yourself, iterate privately when useful, and return the strongest result with the final prompt and selected output paths."
