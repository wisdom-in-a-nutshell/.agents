#!/usr/bin/env bash
# Test-only compatibility shim for `dobby shelf ...` tests.
set -euo pipefail
SKILL_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
cmd="${1:-}"
if [[ -z "$cmd" ]]; then
  echo "usage: dobby-shim shelf ..." >&2
  exit 2
fi
shift
case "$cmd" in
  shelf) exec "$SKILL_DIR/scripts/dobby-shelf" "$@" ;;
  *) echo "unknown dobby group: $cmd" >&2; exit 2 ;;
esac
