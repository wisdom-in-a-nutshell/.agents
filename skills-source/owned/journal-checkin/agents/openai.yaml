interface:
  display_name: "Journal Check-In"
  short_description: "Structured journaling and journal reading"
  default_prompt: "Use $journal-checkin to run a morning, evening, night, or general check-in, store it, or read/query recent journal entries."
