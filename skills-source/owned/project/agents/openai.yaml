interface:
  display_name: "Project"
  short_description: "Create, resume, and close durable project trackers."
  default_prompt: "Use $project to create or resume the repo project tracker, refresh the current batch, execute and validate the work, then archive the complete project tree cleanly when done."
