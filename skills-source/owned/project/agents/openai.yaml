interface:
  display_name: "Project"
  short_description: "Create, resume, and drive long-running project trackers."
  default_prompt: "Use $project to create or resume the repo project tracker, refresh the current batch, execute work, validate progress, and keep tracker resources current."
