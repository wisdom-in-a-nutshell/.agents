interface:
  display_name: "Skill Router"
  short_description: "Route skill placement in .agents: owned, external, or repo-local."
  default_prompt: "Apply .agents AGENTS.md policy, choose placement, update registry/source paths, and run sync/check when registry changes."
