interface:
  display_name: "WIN AIP Contract Sync"
  short_description: "Sync WIN contracts locally into AIP and apply frontend wiring."
  default_prompt: "Use $win-aip-contract-sync to update WIN-to-AIP contracts locally, apply aipodcasting frontend wiring, and validate generated contract consumers."
