interface:
  display_name: "WIN AIP Contract Sync"
  short_description: "Sync WIN contracts locally into AIP and apply frontend wiring."
  default_prompt: "Update WIN -> AIP contracts locally, apply any needed aipodcasting frontend wiring, and validate the generated contract consumers."
