interface:
  display_name: "Media Toolkit"
  short_description: "Upload, transcribe, and process media"
  default_prompt: "Use $media-toolkit to transcribe or process this local file or media URL and return structured results."
