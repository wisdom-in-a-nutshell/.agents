interface:
  display_name: "Media Toolkit"
  short_description: "Run WIN media tools through API"
  default_prompt: "Use $media-toolkit to run WIN transcription, transform, matting, or job status checks from a local file, media URL, or job_id."
