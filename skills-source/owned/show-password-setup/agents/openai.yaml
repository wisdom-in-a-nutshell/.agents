interface:
  display_name: "Show Password Setup"
  short_description: "Set or rotate show-specific access passwords."
  default_prompt: "Use $show-password-setup to set up or rotate PASSWORD_SHOW_<SHOW> in Key Vault and the generated local production env, reload the Mac frontend, then confirm the exact variable name used."
