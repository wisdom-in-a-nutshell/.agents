interface:
  display_name: "Show Password Setup (Deprecated)"
  short_description: "Emergency rotation before Access cutover"
  default_prompt: "Use $show-password-setup only to rotate an already configured show password while APP_ACCESS_MODE=password; refuse new show setup and route new access to the whole-app Cloudflare Access cutover."
