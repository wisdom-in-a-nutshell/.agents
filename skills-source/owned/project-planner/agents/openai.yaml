interface:
  display_name: "Project Planner (Legacy)"
  short_description: "Legacy alias for the unified project tracker workflow."
  default_prompt: "Legacy flow: create or refresh docs/projects/<project>/tasks.md using the unified project template, then prefer the $project skill for continuation."
